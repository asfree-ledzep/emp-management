<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.emp.mapper.HolidayMapper">

    <resultMap id="holidayResultMap" type="com.example.emp.model.Holiday">
        <id     property="holidayId"   column="HOLIDAY_ID"/>
        <result property="holidayDate" column="HOLIDAY_DATE"/>
        <result property="holidayName" column="HOLIDAY_NAME"/>
        <result property="createdBy"   column="CREATED_BY"/>
    </resultMap>

    <!-- 연도별 전체 조회 -->
    <select id="findByYear" resultMap="holidayResultMap">
        SELECT HOLIDAY_ID,
               TO_CHAR(HOLIDAY_DATE, 'YYYY-MM-DD') AS HOLIDAY_DATE,
               HOLIDAY_NAME,
               CREATED_BY
          FROM HOLIDAY
         WHERE EXTRACT(YEAR FROM HOLIDAY_DATE) = #{year}
         ORDER BY HOLIDAY_DATE
    </select>

    <!-- 연월별 조회 (챗봇용) -->
    <select id="findByYearMonth" resultMap="holidayResultMap">
        SELECT HOLIDAY_ID,
               TO_CHAR(HOLIDAY_DATE, 'YYYY-MM-DD') AS HOLIDAY_DATE,
               HOLIDAY_NAME,
               CREATED_BY
          FROM HOLIDAY
         WHERE EXTRACT(YEAR  FROM HOLIDAY_DATE) = #{year}
           AND EXTRACT(MONTH FROM HOLIDAY_DATE) = #{month}
         ORDER BY HOLIDAY_DATE
    </select>

    <!-- 등록 -->
    <insert id="insert" parameterType="com.example.emp.model.Holiday">
        <selectKey keyProperty="holidayId" resultType="long" order="BEFORE">
            SELECT HOLIDAY_SEQ.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO HOLIDAY (HOLIDAY_ID, HOLIDAY_DATE, HOLIDAY_NAME, CREATED_BY)
        VALUES (#{holidayId}, TO_DATE(#{holidayDate}, 'YYYY-MM-DD'), #{holidayName}, #{createdBy})
    </insert>

    <!-- 삭제 -->
    <delete id="delete" parameterType="long">
        DELETE FROM HOLIDAY WHERE HOLIDAY_ID = #{holidayId}
    </delete>

</mapper>
